import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  errorWrapper: {
    width: '100%',
    position: 'relative'
  },
  errorMessage: {
    marginLeft: 20,
    color: 'red'
  }
});

export default styles;
